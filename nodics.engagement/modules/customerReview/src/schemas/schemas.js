/*
    Nodics - Enterprice Micro-Services Management Framework

    Copyright (c) 2026 Nodics All rights reserved.

    This software is governed by the Nodics Source-Available Commercial License.
    You may use, copy, modify, deploy, or distribute it only as permitted by the
    root LICENSE file or a separate written agreement with Nodics.

 */
/** @module customerReview/src/schemas/schemas @description Defines review evidence, immutable versions, authenticity, moderation, responses, abuse, appeals, and CRES migration records. @layer schema @owner customerReview @override Later modules may add governed fields without weakening tenant, integrity, disclosure, or audit guarantees. */
module.exports = { customerReview: {
    customerReview: Object.assign({ super: 'base', model: true, schemaPolicies: ['customerOwned'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, ownerId: { type: 'string', required: true , description: 'Identifies the principal or business owner responsible for this record.'}, targetType: { type: 'string', required: true , description: 'Classifies this record by target type for validation and business handling.'}, targetCode: { type: 'string', required: true , description: 'Stores the target code used to classify, link, or resolve this record.'}, orderCode: { type: 'string', required: false , description: 'Stores the order code used to classify, link, or resolve this record.'}, orderEntryCode: { type: 'string', required: false , description: 'Stores the order entry code used to classify, link, or resolve this record.'},
        overallRating: { type: 'number', required: false , description: 'Stores the numeric overall rating used by this record.'}, dimensionRatings: { type: 'object', required: false , description: 'Stores structured dimension ratings details used by this record.'}, title: { type: 'string', required: false , description: 'Stores the title value used by this record.'}, body: { type: 'string', required: false , description: 'Stores the body value used by this record.'}, advantages: { type: 'array', required: false , description: 'Lists the advantages associated with this record.'}, disadvantages: { type: 'array', required: false , description: 'Lists the disadvantages associated with this record.'}, recommendation: { type: 'bool', required: false , description: 'Indicates whether recommendation applies for this record.'}, structuredAnswers: { type: 'object', required: false , description: 'Stores structured structured answers details used by this record.'}, mediaCodes: { type: 'array', required: false , description: 'Lists media codes used to classify, link, or resolve this record.'},
        site: { type: 'string', required: true , description: 'Stores the site value used by this record.'}, locale: { type: 'string', required: true , description: 'Stores the locale code used for language, formatting, and regional behavior.'}, channel: { type: 'string', required: true , description: 'Stores the channel value used by this record.'}, status: { type: 'string', required: true, enum: ['DRAFT', 'SUBMITTED', 'PENDING_MODERATION', 'QUARANTINED', 'APPROVED', 'REJECTED', 'HIDDEN', 'WITHDRAWN', 'ARCHIVED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, moderationMode: { type: 'string', required: true, enum: ['PRE', 'POST', 'HYBRID'] , description: 'Selects the moderation mode value used to drive validation, filtering, and business behavior.'},
        revision: { type: 'int', required: true, default: 0 , description: 'Tracks the business revision used for governance, review, and optimistic update checks.'}, submittedAt: { type: 'date', required: false , description: 'Records when the submitted event or value applies.'}, withdrawnAt: { type: 'date', required: false , description: 'Records when the withdrawn event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerReviewVersion: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, reviewCode: { type: 'string', required: true , description: 'Stores the review code used to classify, link, or resolve this record.'}, version: { type: 'int', required: true , description: 'Stores the numeric version used by this record.'}, title: { type: 'string', required: false , description: 'Stores the title value used by this record.'}, body: { type: 'string', required: false , description: 'Stores the body value used by this record.'}, overallRating: { type: 'number', required: false , description: 'Stores the numeric overall rating used by this record.'}, dimensionRatings: { type: 'object', required: false , description: 'Stores structured dimension ratings details used by this record.'}, mediaCodes: { type: 'array', required: false , description: 'Lists media codes used to classify, link, or resolve this record.'},
        changeReason: { type: 'string', required: true , description: 'Stores the change reason value used by this record.'}, status: { type: 'string', required: true, enum: ['CURRENT', 'SUPERSEDED', 'WITHDRAWN'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, contentHash: { type: 'string', required: true , description: 'Stores the content hash value used by this record.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}, createdAt: { type: 'date', required: true , description: 'Records when the created event or value applies.'}
    } }),
    customerReviewAuthenticityEvidence: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, reviewCode: { type: 'string', required: true , description: 'Stores the review code used to classify, link, or resolve this record.'}, interactionType: { type: 'string', required: true, enum: ['PURCHASE', 'EXPERIENCE', 'GUEST_VERIFICATION', 'IMPORT', 'NONE'] , description: 'Classifies this record by interaction type for validation and business handling.'}, verificationStatus: { type: 'string', required: true, enum: ['PENDING', 'VERIFIED', 'UNVERIFIED', 'FAILED'] , description: 'Selects the verification status value used to drive validation, filtering, and business behavior.'}, sourceModule: { type: 'string', required: false , description: 'Stores the source module value used by this record.'}, sourceRecordCode: { type: 'string', required: false , description: 'Stores the source record code used to classify, link, or resolve this record.'},
        incentiveType: { type: 'string', required: false , description: 'Classifies this record by incentive type for validation and business handling.'}, sentimentConditioned: { type: 'bool', required: true, default: false , description: 'Indicates whether sentiment conditioned applies for this record.'}, samplingDisclosure: { type: 'string', required: false , description: 'Stores the sampling disclosure value used by this record.'}, materialRelationship: { type: 'string', required: false , description: 'Stores the material relationship value used by this record.'}, aiAssistance: { type: 'string', required: false , description: 'Stores the ai assistance value used by this record.'}, provenance: { type: 'object', required: true , description: 'Stores structured provenance details used by this record.'}, policyVersion: { type: 'string', required: true , description: 'Stores the policy version value used by this record.'}, verifiedAt: { type: 'date', required: false , description: 'Records when the verified event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerReviewModeration: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, reviewCode: { type: 'string', required: true , description: 'Stores the review code used to classify, link, or resolve this record.'}, reviewVersion: { type: 'int', required: true , description: 'Stores the numeric review version used by this record.'}, action: { type: 'string', required: true, enum: ['SUBMIT', 'APPROVE', 'REJECT', 'FLAG', 'QUARANTINE', 'MARK_SPAM', 'HIDE', 'RESTORE', 'ARCHIVE', 'WITHDRAW', 'REQUEST_CORRECTION', 'ESCALATE'] , description: 'Selects the action value used to drive validation, filtering, and business behavior.'}, fromStatus: { type: 'string', required: true , description: 'Stores the from status value used by this record.'}, toStatus: { type: 'string', required: true , description: 'Stores the to status value used by this record.'},
        reasonCode: { type: 'string', required: false , description: 'Stores the reason code used to classify, link, or resolve this record.'}, policyViolation: { type: 'bool', required: true, default: false , description: 'Indicates whether policy violation applies for this record.'}, sentiment: { type: 'string', required: false , description: 'Stores the sentiment value used by this record.'}, internalNotes: { type: 'string', required: false , description: 'Stores the internal notes value used by this record.'}, customerReason: { type: 'string', required: false , description: 'Stores the customer reason value used by this record.'}, escalationType: { type: 'string', required: false , description: 'Classifies this record by escalation type for validation and business handling.'}, actorId: { type: 'string', required: true , description: 'Stores the actor identifier used to correlate this record.'}, evidence: { type: 'object', required: false , description: 'Stores structured evidence details used by this record.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}, occurredAt: { type: 'date', required: true , description: 'Records when the occurred event or value applies.'}
    } }),
    customerReviewResponse: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, reviewCode: { type: 'string', required: true , description: 'Stores the review code used to classify, link, or resolve this record.'}, version: { type: 'int', required: true , description: 'Stores the numeric version used by this record.'}, body: { type: 'string', required: true , description: 'Stores the body value used by this record.'}, authorId: { type: 'string', required: true , description: 'Stores the author identifier used to correlate this record.'}, teamCode: { type: 'string', required: false , description: 'Stores the team code used to classify, link, or resolve this record.'}, status: { type: 'string', required: true, enum: ['DRAFT', 'PENDING_APPROVAL', 'APPROVED', 'PUBLISHED', 'HIDDEN', 'WITHDRAWN', 'SUPERSEDED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, approvedBy: { type: 'string', required: false , description: 'Stores the approved by value used by this record.'}, approvedAt: { type: 'date', required: false , description: 'Records when the approved event or value applies.'}, publishedAt: { type: 'date', required: false , description: 'Records when the published event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerReviewAbuseReport: Object.assign({ super: 'base', model: true, schemaPolicies: ['customerOwned'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, reviewCode: { type: 'string', required: true , description: 'Stores the review code used to classify, link, or resolve this record.'}, reporterId: { type: 'string', required: true , description: 'Stores the reporter identifier used to correlate this record.'}, reasonCode: { type: 'string', required: true , description: 'Stores the reason code used to classify, link, or resolve this record.'}, comment: { type: 'string', required: false , description: 'Stores the comment value used by this record.'}, evidence: { type: 'object', required: false , description: 'Stores structured evidence details used by this record.'}, status: { type: 'string', required: true, enum: ['OPEN', 'UNDER_INVESTIGATION', 'ACTIONED', 'DISMISSED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, resolutionCode: { type: 'string', required: false , description: 'Stores the resolution code used to classify, link, or resolve this record.'}, resolvedBy: { type: 'string', required: false , description: 'Stores the resolved by value used by this record.'}, resolvedAt: { type: 'date', required: false , description: 'Records when the resolved event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerReviewAppeal: Object.assign({ super: 'base', model: true, schemaPolicies: ['customerOwned'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, reviewCode: { type: 'string', required: true , description: 'Stores the review code used to classify, link, or resolve this record.'}, ownerId: { type: 'string', required: true , description: 'Identifies the principal or business owner responsible for this record.'}, moderationCode: { type: 'string', required: true , description: 'Stores the moderation code used to classify, link, or resolve this record.'}, reason: { type: 'string', required: true , description: 'Stores the reason value used by this record.'}, status: { type: 'string', required: true, enum: ['OPEN', 'UNDER_REVIEW', 'UPHELD', 'OVERTURNED', 'CLOSED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, decisionReason: { type: 'string', required: false , description: 'Stores the decision reason value used by this record.'}, decidedBy: { type: 'string', required: false , description: 'Stores the decided by value used by this record.'}, decidedAt: { type: 'date', required: false , description: 'Records when the decided event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerReviewMigration: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, legacyModule: { type: 'string', required: true , description: 'Stores the legacy module value used by this record.'}, legacySchema: { type: 'string', required: true , description: 'Stores the legacy schema value used by this record.'}, legacyCode: { type: 'string', required: true , description: 'Stores the legacy code used to classify, link, or resolve this record.'}, targetSchema: { type: 'string', required: true , description: 'Stores the target schema value used by this record.'}, targetCode: { type: 'string', required: false , description: 'Stores the target code used to classify, link, or resolve this record.'}, sourceHash: { type: 'string', required: true , description: 'Stores a fingerprint of the source data used to detect changes or duplicates.'}, strategy: { type: 'string', required: true, enum: ['CUTOVER', 'DRY_RUN'] , description: 'Selects the strategy value used to drive validation, filtering, and business behavior.'}, status: { type: 'string', required: true, enum: ['PLANNED', 'MIGRATED', 'SKIPPED', 'FAILED', 'ROLLED_BACK', 'RECONCILED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, mappingVersion: { type: 'string', required: true , description: 'Stores the mapping version value used by this record.'}, errors: { type: 'array', required: false , description: 'Lists the errors associated with this record.'}, migratedAt: { type: 'date', required: false , description: 'Records when the migrated event or value applies.'}, rolledBackAt: { type: 'date', required: false , description: 'Records when the rolled back event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } })
    , customerReviewProjection: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, reviewCode: { type: 'string', required: true , description: 'Stores the review code used to classify, link, or resolve this record.'}, reviewVersion: { type: 'int', required: true , description: 'Stores the numeric review version used by this record.'}, targetType: { type: 'string', required: true , description: 'Classifies this record by target type for validation and business handling.'}, targetCode: { type: 'string', required: true , description: 'Stores the target code used to classify, link, or resolve this record.'}, variantCode: { type: 'string', required: false , description: 'Stores the variant code used to classify, link, or resolve this record.'}, overallRating: { type: 'number', required: false , description: 'Stores the numeric overall rating used by this record.'}, dimensionRatings: { type: 'object', required: false , description: 'Stores structured dimension ratings details used by this record.'}, title: { type: 'string', required: false , description: 'Stores the title value used by this record.'}, body: { type: 'string', required: false , description: 'Stores the body value used by this record.'}, recommendation: { type: 'bool', required: false , description: 'Indicates whether recommendation applies for this record.'}, media: { type: 'array', required: false , description: 'Lists the media associated with this record.'}, businessResponse: { type: 'object', required: false , description: 'Stores structured business response details used by this record.'}, authenticity: { type: 'object', required: true , description: 'Stores structured authenticity details used by this record.'}, disclosures: { type: 'array', required: false , description: 'Lists the disclosures associated with this record.'}, helpfulCount: { type: 'int', required: true, default: 0 , description: 'Stores the helpful count used for validation, calculation, or operational decisions.'}, unhelpfulCount: { type: 'int', required: true, default: 0 , description: 'Stores the unhelpful count used for validation, calculation, or operational decisions.'}, site: { type: 'string', required: true , description: 'Stores the site value used by this record.'}, locale: { type: 'string', required: true , description: 'Stores the locale code used for language, formatting, and regional behavior.'}, channel: { type: 'string', required: true , description: 'Stores the channel value used by this record.'}, status: { type: 'string', required: true, enum: ['PUBLISHED', 'HIDDEN', 'WITHDRAWN', 'ANONYMIZED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, publishedAt: { type: 'date', required: false , description: 'Records when the published event or value applies.'}, hiddenAt: { type: 'date', required: false , description: 'Records when the hidden event or value applies.'}, policyVersion: { type: 'string', required: true , description: 'Stores the policy version value used by this record.'}, sourceHash: { type: 'string', required: true , description: 'Stores a fingerprint of the source data used to detect changes or duplicates.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } })
    , customerReviewAggregate: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, targetType: { type: 'string', required: true , description: 'Classifies this record by target type for validation and business handling.'}, targetCode: { type: 'string', required: true , description: 'Stores the target code used to classify, link, or resolve this record.'}, variantCode: { type: 'string', required: false , description: 'Stores the variant code used to classify, link, or resolve this record.'}, site: { type: 'string', required: true , description: 'Stores the site value used by this record.'}, locale: { type: 'string', required: false , description: 'Stores the locale code used for language, formatting, and regional behavior.'}, count: { type: 'int', required: true , description: 'Stores the numeric count used by this record.'}, sum: { type: 'number', required: true , description: 'Stores the numeric sum used by this record.'}, average: { type: 'number', required: true , description: 'Stores the numeric average used by this record.'}, distribution: { type: 'object', required: true , description: 'Stores structured distribution details used by this record.'}, dimensions: { type: 'object', required: true , description: 'Stores structured dimensions details used by this record.'}, verifiedCount: { type: 'int', required: true , description: 'Stores the verified count used for validation, calculation, or operational decisions.'}, unverifiedCount: { type: 'int', required: true , description: 'Stores the unverified count used for validation, calculation, or operational decisions.'}, policyVersion: { type: 'string', required: true , description: 'Stores the policy version value used by this record.'}, calculationVersion: { type: 'string', required: true , description: 'Stores the calculation version value used by this record.'}, sourceHash: { type: 'string', required: true , description: 'Stores a fingerprint of the source data used to detect changes or duplicates.'}, calculatedAt: { type: 'date', required: true , description: 'Records when the calculated event or value applies.'}, status: { type: 'string', required: true, enum: ['CURRENT', 'STALE', 'REBUILDING', 'DRIFTED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } })
    , customerReviewHelpfulness: Object.assign({ super: 'base', model: true, schemaPolicies: ['customerOwned'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, reviewCode: { type: 'string', required: true , description: 'Stores the review code used to classify, link, or resolve this record.'}, ownerId: { type: 'string', required: true , description: 'Identifies the principal or business owner responsible for this record.'}, helpful: { type: 'bool', required: true , description: 'Indicates whether helpful applies for this record.'}, status: { type: 'string', required: true, enum: ['ACTIVE', 'WITHDRAWN'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, revision: { type: 'int', required: true, default: 0 , description: 'Tracks the business revision used for governance, review, and optimistic update checks.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } })
    , customerReviewRequest: Object.assign({ super: 'base', model: true, schemaPolicies: ['customerOwned'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, ownerId: { type: 'string', required: true , description: 'Identifies the principal or business owner responsible for this record.'}, eligibilityType: { type: 'string', required: true, enum: ['PURCHASE', 'SERVICE', 'EXPERIENCE'] , description: 'Classifies this record by eligibility type for validation and business handling.'}, sourceModule: { type: 'string', required: true , description: 'Stores the source module value used by this record.'}, sourceRecordCode: { type: 'string', required: true , description: 'Stores the source record code used to classify, link, or resolve this record.'}, targetCodes: { type: 'array', required: true , description: 'Lists target codes used to classify, link, or resolve this record.'}, channel: { type: 'string', required: true, enum: ['EMAIL', 'SMS', 'ACCOUNT', 'QR', 'IN_APP'] , description: 'Selects the channel value used to drive validation, filtering, and business behavior.'}, status: { type: 'string', required: true, enum: ['PLANNED', 'OFFERED', 'DELIVERED', 'OPENED', 'STARTED', 'COMPLETED', 'EXPIRED', 'SUPPRESSED', 'FAILED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, eligibleAt: { type: 'date', required: true , description: 'Records when the eligible event or value applies.'}, offeredAt: { type: 'date', required: false , description: 'Records when the offered event or value applies.'}, expiresAt: { type: 'date', required: true , description: 'Records when the expires event or value applies.'}, reminderCount: { type: 'int', required: true, default: 0 , description: 'Stores the reminder count used for validation, calculation, or operational decisions.'}, nextReminderAt: { type: 'date', required: false , description: 'Records when the next reminder event or value applies.'}, suppressionReason: { type: 'string', required: false , description: 'Stores the suppression reason value used by this record.'}, policyVersion: { type: 'string', required: true , description: 'Stores the policy version value used by this record.'}, idempotencyKey: { type: 'string', required: true , description: 'Stores the idempotency key value used by this record.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } })
    , customerReviewSession: Object.assign({ super: 'base', model: true, schemaPolicies: ['customerOwned'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, ownerId: { type: 'string', required: true , description: 'Identifies the principal or business owner responsible for this record.'}, requestCode: { type: 'string', required: true , description: 'Stores the request code used to classify, link, or resolve this record.'}, targetCodes: { type: 'array', required: true , description: 'Lists target codes used to classify, link, or resolve this record.'}, completedTargetCodes: { type: 'array', required: false , description: 'Lists completed target codes used to classify, link, or resolve this record.'}, status: { type: 'string', required: true, enum: ['OPEN', 'IN_PROGRESS', 'COMPLETED', 'EXPIRED', 'ABANDONED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, startedAt: { type: 'date', required: false , description: 'Records when the started event or value applies.'}, completedAt: { type: 'date', required: false , description: 'Records when the completed event or value applies.'}, expiresAt: { type: 'date', required: true , description: 'Records when the expires event or value applies.'}, revision: { type: 'int', required: true, default: 0 , description: 'Tracks the business revision used for governance, review, and optimistic update checks.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } })
    , customerReviewAcquisitionEvent: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, requestCode: { type: 'string', required: true , description: 'Stores the request code used to classify, link, or resolve this record.'}, sessionCode: { type: 'string', required: false , description: 'Stores the session code used to classify, link, or resolve this record.'}, eventType: { type: 'string', required: true, enum: ['ELIGIBLE', 'OFFERED', 'DELIVERED', 'OPENED', 'STARTED', 'COMPLETED', 'EXPIRED', 'SUPPRESSED', 'FAILED'] , description: 'Classifies this record by event type for validation and business handling.'}, channel: { type: 'string', required: false , description: 'Stores the channel value used by this record.'}, providerCode: { type: 'string', required: false , description: 'Stores the provider code used to classify, link, or resolve this record.'}, providerReference: { type: 'string', required: false , description: 'Stores provider-specific reference data needed to resolve this record.'}, occurredAt: { type: 'date', required: true , description: 'Records when the occurred event or value applies.'}, evidence: { type: 'object', required: false , description: 'Stores structured evidence details used by this record.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } })
    , customerReviewSyndication: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { enabled: false }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, direction: { type: 'string', required: true, enum: ['IMPORT', 'EXPORT'] , description: 'Selects the direction value used to drive validation, filtering, and business behavior.'}, providerCode: { type: 'string', required: true , description: 'Stores the provider code used to classify, link, or resolve this record.'}, externalReviewId: { type: 'string', required: true , description: 'Stores the external review identifier used to correlate this record.'}, externalTargetId: { type: 'string', required: true , description: 'Stores the external target identifier used to correlate this record.'}, targetType: { type: 'string', required: true , description: 'Classifies this record by target type for validation and business handling.'}, targetCode: { type: 'string', required: true , description: 'Stores the target code used to classify, link, or resolve this record.'}, reviewCode: { type: 'string', required: false , description: 'Stores the review code used to classify, link, or resolve this record.'}, originUrl: { type: 'string', required: false , description: 'Stores the origin url value used by this record.'}, licenseCode: { type: 'string', required: true , description: 'Stores the license code used to classify, link, or resolve this record.'}, disclosure: { type: 'string', required: true , description: 'Stores the disclosure value used by this record.'}, sourceHash: { type: 'string', required: true , description: 'Stores a fingerprint of the source data used to detect changes or duplicates.'}, mappingVersion: { type: 'string', required: true , description: 'Stores the mapping version value used by this record.'}, moderationEvidence: { type: 'object', required: true , description: 'Stores structured moderation evidence details used by this record.'}, status: { type: 'string', required: true, enum: ['RECEIVED', 'VALIDATED', 'QUARANTINED', 'IMPORTED', 'EXPORTED', 'FAILED', 'RECONCILED', 'WITHDRAWN'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, errors: { type: 'array', required: false , description: 'Lists the errors associated with this record.'}, lastReconciledAt: { type: 'date', required: false , description: 'Records when the last reconciled event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } })
} };

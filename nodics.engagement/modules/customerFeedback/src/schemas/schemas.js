/*
    Nodics - Enterprice Micro-Services Management Framework

    Copyright (c) 2026 Nodics All rights reserved.

    This software is governed by the Nodics Source-Available Commercial License.
    You may use, copy, modify, deploy, or distribute it only as permitted by the
    root LICENSE file or a separate written agreement with Nodics.

 */
/** @module customerFeedback/src/schemas/schemas @description Defines feedback, follow-up, resolution, handoff, survey, and derived-insight evidence. @layer schema @owner customerFeedback @override Later modules may add governed fields without weakening tenant, ownership, traceability, or correction guarantees. */
module.exports = { customerFeedback: {
    customerFeedback: Object.assign({ super: 'base', model: true, schemaPolicies: ['customerOwned'], service: { enabled: true }, router: { groups: { schemaOperations: true }, enabled: true }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, ownerId: { type: 'string', required: false , description: 'Identifies the principal or business owner responsible for this record.'}, anonymous: { type: 'bool', required: true, default: false , description: 'Indicates whether anonymous applies for this record.'}, type: { type: 'string', required: true, enum: ['SUGGESTION', 'COMPLAINT', 'EXPERIENCE', 'SURVEY_RESPONSE', 'PRAISE', 'OTHER'] , description: 'Selects the type value used to drive validation, filtering, and business behavior.'}, targetType: { type: 'string', required: false , description: 'Classifies this record by target type for validation and business handling.'}, targetCode: { type: 'string', required: false , description: 'Stores the target code used to classify, link, or resolve this record.'}, subject: { type: 'string', required: false , description: 'Stores the subject value used by this record.'}, message: { type: 'string', required: false , description: 'Stores the message value used by this record.'}, structuredAnswers: { type: 'object', required: false , description: 'Stores structured structured answers details used by this record.'}, scores: { type: 'object', required: false , description: 'Stores structured scores details used by this record.'}, desiredOutcome: { type: 'string', required: false , description: 'Stores the desired outcome value used by this record.'}, attachmentCodes: { type: 'array', required: false , description: 'Lists attachment codes used to classify, link, or resolve this record.'}, preferredChannel: { type: 'string', required: false , description: 'Stores the preferred channel value used by this record.'}, status: { type: 'string', required: true, enum: ['RECEIVED', 'TRIAGED', 'ASSIGNED', 'IN_PROGRESS', 'WAITING_CUSTOMER', 'WAITING_INTERNAL', 'ESCALATED', 'RESOLVED', 'CLOSED', 'REJECTED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, category: { type: 'string', required: false , description: 'Stores the category value used by this record.'}, topic: { type: 'string', required: false , description: 'Stores the topic value used by this record.'}, sentiment: { type: 'string', required: false , description: 'Stores the sentiment value used by this record.'}, priority: { type: 'string', required: true , description: 'Stores the priority used to order or resolve this record against competing records.'}, severity: { type: 'string', required: false , description: 'Stores the severity value used by this record.'}, queueCode: { type: 'string', required: false , description: 'Stores the queue code used to classify, link, or resolve this record.'}, assigneeId: { type: 'string', required: false , description: 'Stores the assignee identifier used to correlate this record.'}, dueAt: { type: 'date', required: false , description: 'Records when the due event or value applies.'}, revision: { type: 'int', required: true, default: 0 , description: 'Tracks the business revision used for governance, review, and optimistic update checks.'}, formDefinitionCode: { type: 'string', required: false , description: 'Stores the form definition code used to classify, link, or resolve this record.'}, formVersion: { type: 'int', required: false , description: 'Stores the numeric form version used by this record.'}, submittedAt: { type: 'date', required: true , description: 'Records when the submitted event or value applies.'}, resolvedAt: { type: 'date', required: false , description: 'Records when the resolved event or value applies.'}, closedAt: { type: 'date', required: false , description: 'Records when the closed event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerFeedbackClassification: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { groups: { schemaOperations: true }, enabled: true }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, feedbackCode: { type: 'string', required: true , description: 'Stores the feedback code used to classify, link, or resolve this record.'}, category: { type: 'string', required: false , description: 'Stores the category value used by this record.'}, topic: { type: 'string', required: false , description: 'Stores the topic value used by this record.'}, sentiment: { type: 'string', required: false , description: 'Stores the sentiment value used by this record.'}, priority: { type: 'string', required: false , description: 'Stores the priority used to order or resolve this record against competing records.'}, severity: { type: 'string', required: false , description: 'Stores the severity value used by this record.'}, source: { type: 'string', required: true, enum: ['RULE', 'OPERATOR', 'AI', 'IMPORT'] , description: 'Selects the source value used to drive validation, filtering, and business behavior.'}, confidence: { type: 'number', required: true , description: 'Stores the numeric confidence used by this record.'}, policyVersion: { type: 'string', required: true , description: 'Stores the policy version value used by this record.'}, modelReference: { type: 'string', required: false , description: 'Stores the model reference value used by this record.'}, correctedFromCode: { type: 'string', required: false , description: 'Stores the corrected from code used to classify, link, or resolve this record.'}, correctedBy: { type: 'string', required: false , description: 'Stores the corrected by value used by this record.'}, evidence: { type: 'object', required: false , description: 'Stores structured evidence details used by this record.'}, classifiedAt: { type: 'date', required: true , description: 'Records when the classified event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerFeedbackFollowUp: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { groups: { schemaOperations: true }, enabled: true }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, feedbackCode: { type: 'string', required: true , description: 'Stores the feedback code used to classify, link, or resolve this record.'}, channel: { type: 'string', required: true , description: 'Stores the channel value used by this record.'}, status: { type: 'string', required: true, enum: ['OFFERED', 'ATTEMPTED', 'CONTACTED', 'RESOLVED', 'ACCEPTED', 'NO_RESPONSE', 'FAILED', 'SUPPRESSED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, attempt: { type: 'int', required: true , description: 'Stores the numeric attempt used by this record.'}, providerReference: { type: 'string', required: false , description: 'Stores provider-specific reference data needed to resolve this record.'}, outcomeCode: { type: 'string', required: false , description: 'Stores the outcome code used to classify, link, or resolve this record.'}, occurredAt: { type: 'date', required: true , description: 'Records when the occurred event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerFeedbackResolution: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { groups: { schemaOperations: true }, enabled: true }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, feedbackCode: { type: 'string', required: true , description: 'Stores the feedback code used to classify, link, or resolve this record.'}, version: { type: 'int', required: true , description: 'Stores the numeric version used by this record.'}, outcomeCode: { type: 'string', required: true , description: 'Stores the outcome code used to classify, link, or resolve this record.'}, summary: { type: 'string', required: true , description: 'Stores the summary value used by this record.'}, customerConfirmed: { type: 'bool', required: false , description: 'Indicates whether customer confirmed applies for this record.'}, resolvedBy: { type: 'string', required: true , description: 'Stores the resolved by value used by this record.'}, resolvedAt: { type: 'date', required: true , description: 'Records when the resolved event or value applies.'}, status: { type: 'string', required: true, enum: ['PROPOSED', 'RESOLVED', 'ACCEPTED', 'REOPENED', 'SUPERSEDED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerFeedbackHandoff: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { groups: { schemaOperations: true }, enabled: true }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, feedbackCode: { type: 'string', required: true , description: 'Stores the feedback code used to classify, link, or resolve this record.'}, targetModule: { type: 'string', required: true , description: 'Stores the target module value used by this record.'}, targetOperation: { type: 'string', required: true , description: 'Stores the target operation value used by this record.'}, externalReference: { type: 'string', required: false , description: 'Stores the external reference value used by this record.'}, idempotencyKey: { type: 'string', required: true , description: 'Stores the idempotency key value used by this record.'}, status: { type: 'string', required: true, enum: ['PENDING', 'IN_PROGRESS', 'SUCCEEDED', 'RETRY_PENDING', 'FAILED', 'DEAD_LETTER', 'RECONCILED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, attemptCount: { type: 'int', required: true, default: 0 , description: 'Stores the attempt count used for validation, calculation, or operational decisions.'}, nextAttemptAt: { type: 'date', required: false , description: 'Records when the next attempt event or value applies.'}, lastOutcome: { type: 'object', required: false , description: 'Stores structured last outcome details used by this record.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } }),
    customerFeedbackInsight: Object.assign({ super: 'base', model: true, schemaPolicies: ['operational'], service: { enabled: true }, router: { groups: { schemaOperations: true }, enabled: true }, cache: { enabled: false }, event: { enabled: false }, search: { enabled: false } }, { definition: {
        tenant: { type: 'string', required: true , description: 'Identifies the runtime tenant partition that scopes this record.'}, insightType: { type: 'string', required: true, enum: ['TOPIC', 'CLUSTER', 'TREND', 'ANOMALY', 'SUMMARY'] , description: 'Classifies this record by insight type for validation and business handling.'}, sourceFeedbackCodes: { type: 'array', required: true , description: 'Lists source feedback codes used to classify, link, or resolve this record.'}, value: { type: 'object', required: true , description: 'Stores structured value details used by this record.'}, confidence: { type: 'number', required: true , description: 'Stores the numeric confidence used by this record.'}, source: { type: 'string', required: true, enum: ['RULE', 'OPERATOR', 'AI'] , description: 'Selects the source value used to drive validation, filtering, and business behavior.'}, policyVersion: { type: 'string', required: true , description: 'Stores the policy version value used by this record.'}, modelReference: { type: 'string', required: false , description: 'Stores the model reference value used by this record.'}, promptVersion: { type: 'string', required: false , description: 'Stores the prompt version value used by this record.'}, status: { type: 'string', required: true, enum: ['PROPOSED', 'ACCEPTED', 'CORRECTED', 'REJECTED', 'STALE', 'DELETED'] , description: 'Tracks the lifecycle state that controls whether this record can be used in business processes.'}, correctedBy: { type: 'string', required: false , description: 'Stores the corrected by value used by this record.'}, correctionReason: { type: 'string', required: false , description: 'Stores the correction reason value used by this record.'}, generatedAt: { type: 'date', required: true , description: 'Records when the generated event or value applies.'}, deletedAt: { type: 'date', required: false , description: 'Records when the deleted event or value applies.'}, correlationId: { type: 'string', required: true , description: 'Stores the correlation identifier used to correlate this record.'}
    } })
} };

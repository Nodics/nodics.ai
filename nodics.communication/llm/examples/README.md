# Communication examples

An Engagement acknowledgement supplies a template code, declared variables, recipient reference, purpose, channel preference, tenant, idempotency key, and correlation ID. Communication validates consent and suppression, renders transiently, uses the configured provider, and returns content-free delivery evidence. A provider outage records bounded retry or fallback; it never rolls back the customer submission.

# flowCore AI Contracts

This folder contains `flowCore` contracts.

Use it for graph validation, definition lifecycle, versioning, execution, retry, task, audit, compensation, and adapter-governance rules. Schemas belong in `flowSchema`; HTTP projection belongs in `flowApi`.

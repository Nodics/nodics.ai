# Workflow Schema Agent Guide

Use `flowSchema` only for process schema contracts, schema-owned utilities, and status definitions.

Do not add routers, controllers, facades, runtime engine services, or Axis UI behavior here.

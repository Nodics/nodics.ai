# flowSchema AI Contracts

This folder contains `flowSchema` contracts.

Use it for process schema, status definition, model naming, generated-artifact, and schema compatibility rules. Runtime lifecycle logic belongs in `flowCore`; API projection belongs in `flowApi`.

# flowApi AI Examples

This folder is reserved for API projection examples.

Examples should show route additions, request mapping, and facade delegation patterns that preserve `flowCore` as the lifecycle authority.

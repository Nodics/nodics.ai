# Loyalty Reward Payment examples

Use `paymentMethod: 'LOYALTY_REWARD'` with `walletCode` and optional `rewardAmount`, `programCode`, and `rewardTypeCode` when checkout should pay with a Loyalty wallet balance.

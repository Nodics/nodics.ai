# Partner overlay contracts

Fixture records must use existing Waste schemas and manifest headers. The owning tests prove later-layer replacement by stable code and reject conflicting ownership. This fixture grants no partner write authority over framework source.

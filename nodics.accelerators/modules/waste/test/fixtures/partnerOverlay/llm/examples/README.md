# Partner overlay examples

The fixture adds a project category and impact profile and overrides configured acceptance/collection data by stable code. Reproduce that pattern in a customer-owned module, preserving manifest checksums and destination authority.

# nexusCore examples

Select `nexus` in a customer WCMS composition and include its source root in runtime discovery. Select `nexusCore` alone for administrative descriptors. Add customer overlays at a later index; never edit shipped accelerator files.

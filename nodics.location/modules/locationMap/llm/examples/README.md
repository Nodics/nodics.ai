# locationMap examples

Generated documentation entry for locationMap.

For a live customization example, change the `repair` label and pin color in
Axis Map Configuration, save, and observe the same category in another open
consumer within the configured refresh interval. Restore the original values
after acceptance. The [shared map contract](../contracts/shared-map-configuration.md)
explains ownership, bounded settings and failure cases.

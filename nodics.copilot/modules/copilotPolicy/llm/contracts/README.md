# copilotPolicy contracts

Mandatory local contract:

- `security-enforcement-contract.md` defines how `copilotPolicy` implements and
  distributes the parent Copilot security-governance contract.

Generated context is descriptive only and must not weaken these contracts.

# mockProvider contracts

The mock adapter is deterministic and credential-free. It exists only for
contract, orchestration, failure, and customization tests and must never be
presented as a production language model.
